#logging.level.org.springframework= = debug {This line is used to see the debug logs and are helpful to see what is happening in the background}
#spring.security.user.name=nav
#spring.security.user.password=nav

jwt.signing.key.secret=mySecret
jwt.get.token.uri=/authenticate
jwt.refresh.token.uri=/refresh
jwt.http.request.header=Authorization
jwt.token.expiration.in.seconds=604800

spring.jpa.show-sql=true
spring.h2.console.enabled=true